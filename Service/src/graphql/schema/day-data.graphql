type DayData {
    id: Int!,
    todayopen: Float,
    yestclose: Float,
    todayclose: Float,
    todaydate: Date,
    created: DateTime
}

type Query {
    DayData: DayDataQuery
}

type DayDataQuery {
    data(id: Int, start: Date, end: Date): [DayData]
}