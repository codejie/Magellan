type BaseInfo {
    id: Int!,
    type: Int!,
    code: String!,
    market: Int
}

type Query {
    # fetchBaseInfo(id: Int): BaseInfo,
    baseInfos: [BaseInfo]
}

type Mutation {
    addBaseInfo(type: Int!, code: String!, market: Int, name: String): Int
}